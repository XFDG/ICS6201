# 冯浩然

电话：(+86)15024999885  邮箱：225010160@link.cuhk.edu.cn

## 教育经历

| 时间 | 学校 | 专业 |
|---|---|---|
| 2025.09-2027.06(预计) | 香港中文大学（深圳） | 集成电路与系统 硕士 |
| 2021.09-2025.06 | 山东大学（985） | 电子/计算机科学与技术 本科 |

获得荣誉：山东大学学业奖学金（前 20%） 山东大学特长奖学金（竞赛创新）

## 实习经历

**2026.05-至今  九坤-至知研究院  大模型系统与高性能计算实习生**

- 面向 8xH200 上的大模型 MoE-RL 训练链路，打通 `verl + Megatron + vLLM + DeepEP/flex` 的 R3 router replay 预验证；定位 vLLM CUDA graph/compiled sampler 路径导致的 `sample_tokens` timeout，固定 eager 规避后完成 baseline/R2/R3 的 5/25/50-step 对照，全部 0 hang、`metric_error=0`。
- 设计并实现训推路由一致性诊断闭环：从 rollout 侧采集 token-layer-topk 维度 `routed_experts`，在 Megatron 训练侧按 response mask 做 router replay，并上报 `topk_overlap`、`route_mismatch_rate`、`valid_token_layer_count` 等指标；多组实验达到 `topk_overlap=1.0`、`mismatch=0.0`，单步有效 token-layer 统计约 2.4M-2.7M。
- 扩展 logprob drift 对照指标，统一 baseline/R2/R3 的 `f_tau_2`、`KL`、`k3_kl`、`abs_diff` 口径；在脱敏实验中 R3 相比 baseline 将 `f_tau_2` 降低约 30x-46x，15-step 独立对照达到 77x 低漂移，并验证 R2 “只记录不回放”基本不改善 drift。
- 面向 H200 MoE/GEMM 热路径开展 Nsight/Nsys profiling 与算子级口径对齐，修正 `T*topk` 全展开 rows 与 DeepEP compact rows (`TK_valid`) 混用导致的 4x-6x 误判；基于 1024 条 forward range 构造本地回放，三个代表 shape 与内部 profile 差异约 `+0.46%/-1.11%/-5.40%`。
- 参与 Quack/CUTLASS grouped GEMM config-level tuning，围绕 `gemm_gated`、`gemm_out` 分析 MFU、latency 与 expert 分布；通过补充 SM90 autotune 候选使目标线上 shape 获得约 `+5.3%` 加速，14 shape x 3 分布整体提升约 `+1.84%`，并确认 pure GEMM 平均 MFU 约 66.5%。

**2026.01-2026.04  摩尔线程  算子与编译器优化实习生**

- 参与 TensorFlow MUSA Extension 插件开发，负责 TensorFlow on MUSA 的算子支持、插件集成、测试验证与图优化；完成 muDNN GELU 接入、GELU fusion 链路修复与 benchmark 建设，解决大图场景下部分 GELU 未融合问题，推动整网 11 个 GELU 全部融合，真实 shape testcase 性能提升约 36.6%。
- 面向整网长跑中的 OOM/随机崩溃问题，独立定位 shape tensor 在 `StridedSlice<int32>` / `Pack<int32>` 链路中错误进入 device path 的根因，重构 HostMemory 路径并完成修复；将 inference 500 轮成功率从约 30% 提升至 1000 轮 100%，并通过 4 万/40 万/80 万轮长跑验证。
- 独立提交并合入官方仓库 PR #57，设计并实现 kernel timing macro 与调试分析机制，构建基于日志、环境变量和测试脚本的轻量级性能定位闭环；将原先依赖生成 msys 文件并借助 mcu 分析的流程，简化为插入宏快速观测 kernel 粗粒度时间占比，定位效率提升约 50%。https://github.com/MooreThreads/tensorflow_musa_extension/pull/57
- 持续推进热点算子优化与稳定性修复：专项优化 `Logical_Or` scalar broadcast 路径并补齐 `Logical_And` 测试，使 `Logical_Or` 平均耗时从 21.2us 降至 10.7us，整网吞吐从 8187.48 提升至 8284.65；同时完成 AddV2 hot path 优化与 `ResourceApplyAdam` graph mode 结果对齐。

## 项目经历

**2025.12-至今  基于 PyTorch Extension 的高性能 LLM 量化推理 Runtime 开发**

- 推理 Runtime 设计：面向 Llama3/Qwen 等大模型部署中的显存瓶颈，设计并实现支持 W8A8 / FP8 / AWQ 等量化策略的轻量级推理 Runtime。
- 核心算子开发：基于 NVIDIA CUTLASS 开发并封装高性能 W8A8 (INT8) GEMM 算子，重构 Transformer 中 Linear 层逻辑，完成量化数据结构设计、内存布局优化及前向计算端到端正确性验证。
- 性能优化演进：使用 Nsight Compute (NCU) 等 profiling 工具分析 kernel 性能瓶颈，持续推进基于 Cutlass EVT 的算子融合以降低 Kernel Launch 开销，并针对 Decode 阶段设计手写 GEMV 算子以优化带宽受限场景下的推理性能。

**2024.03-2024.06  基于 ResNet 的图像分类与半监督学习研究**

- 项目内容：基于 ResNet-18 架构搭建端到端图像分类流水线，深入探究 CNN 卷积计算特性。针对训练数据类别不平衡与标签稀缺痛点，设计了包含半监督学习（Pseudo-labeling）与混合数据增强的优化策略。
- 量化成果：通过上述策略，成功解决了过拟合问题，将模型在测试集上的 Top-1 准确率从 Baseline 的 51.8% 提升至 85.1%。获得 kaggle 比赛第七名的成绩。https://www.kaggle.com/competitions/dl2023autumexp2

## 专业技能

- 技术方向：具备深度学习、异构计算与底层性能优化背景，获得华为 HCIA-AI 认证，关注 AI 系统中的软硬件协同设计与数据吞吐优化。
- 计算机技能：C/C++、Python、CUDA，熟练使用 Codex 等 AI 辅助开发工具进行代码生成、调试与原型验证。
- 英语：IELTS 6.5，CET-6，具备良好的英文技术文档阅读、检索与工程协作能力。
