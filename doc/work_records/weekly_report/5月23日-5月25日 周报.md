完成的事情：
完成 CUTLASS 2.x Grouped GEMM Baseline 全覆盖 Benchmark（Raw GEMM + MoE e2e），测试 EP=(1,8,16,32) × T=(1024~16384)，共 60+ 数据点
完成 EP 专项扩展测试，验证 EP/E_gpu/K_topk 三角约束对 e2e 的影响
完成 CUTLASS 3.x Hopper Grouped GEMM BF16 适配 5 轮编译调试，定位根因为 CUTLASS v4.5.0 BF16 PtrArray schedule 模板兼容性问题
完成 SM90 可调参数文档化（TileShape / ClusterShape / Schedule / NumStages），理解 DeepGEMM 对应调优策略

关键数据：

Raw GEMM 最优单点

| 方向 | EP | T | TFLOPS | MFU |
|------|----|---|--------|-----|
| Up-proj | 16 | 8192 | 418.8 | 42.3% |
| Down-proj | 32 | 8192 | 425.7 | 43.0% |

MoE End-to-End（per-GPU expert 视图）

| EP | E_gpu | K_topk | FWD MFU | BWD MFU |
|----|-------|--------|---------|---------|
| 8 | 8 | 8 | 23.9% | 16.6% |
| 16 | 4 | 4 | 24.7% | 15.8% |
| 32 | 2 | 2 | 26.5% | 15.4% |

未来的目标：
本地 GPU 环境搭建 NVCC 开发头文件，消除 glibc 版本不匹配问题
从 CUTLASS 原版 CMake 编译 BF16 57 示例，确认 CUTLASS 3.x 对 BF16 grouped GEMM 的支持程度
若 CUTLASS 3.x 不支持 BF16，评估 FP8 训练路线或等待 CUTLASS 升级
调优 TileShape/ClusterShape/Schedule 参数，目标 MFU > 60%

碰到的问题：
CUTLASS 3.x GemmUniversalAdapter 模板参数由编译期类型自动生成，BF16 + PtrArray schedule 组合下 CollectiveMma/Epilogue 内部类型不匹配，5 轮修复后仍有残余错误
57_hopper_grouped_gemm 示例仅提供 FP8 版本，BF16 需不同 schedule 组合（KernelPtrArrayTmaWarpSpecializedCooperative + LinearCombination），文档未覆盖
远程编译 edit-compile 周期 >5min，调参效率极低，需要本地 CUDA 开发环境
