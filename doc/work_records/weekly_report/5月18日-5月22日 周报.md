完成的事情：
完成了 DeepGEMM 离线预编译 Stage3 最终轮验证，bundle 从 109 kernel 扩展到 359 kernel（+230%），覆盖 8 个 H200 单卡模型 + 1 个 TP=2 模型，全部 cold=0
完成 wheel-only 交付形态收敛，产出 baseline vllm + deep_gemm + combined 三种 wheel，安装后自动解析包内 precompiled bundle
首次跑通 H200 TP=2 的 collect→merge→verify 闭环（Qwen3-8B-FP8），load 加速 7.7x
实现 union bundle 自动生成脚本（build_hopper_union_bundle.py），支持多模型 merge 和 sha256 一致性检查
修复 gpu0 TP=2 NCCL 环境问题（CUDA13 降级到 CUDA12 NCCL 2.27.5）
完成 LLaMA-Factory 4x H200 训练环境搭建

未来的目标：
GPU 机器编译并运行 grouped_gemm baseline benchmark，记录 cuBLAS 逐 expert 循环的 MFU 基线
实现 Hopper Grouped GEMM：新建 hopper_grouped_gemm.h/cu，使用 CUTLASS 3.x GemmUniversalAdapter + GroupProblemShape API，替换现有 SM90 的 cuBLAS 回退路径
针对 200B MoE 四种典型 GEMM 形状（small-M / medium-M / large-M / large-K backward）做参数调优，目标 MFU > 60%，相比 cuBLAS baseline 加速 1.5x+
扩展 cu_seqlens 接口以兼容 sonic-moe 的 quack 调用格式
sonic-moe 端到端集成测试，NCU profile 验证实际 MFU

碰到的问题：
DeepSeek-V3-0324 模型 642GB，2x H200 装不下，暂时跳过
H100 机器暂不可用，H100 覆盖 pending
默认 vLLM 路径仍可能被 FlashInfer / TRTLLM 抢走，当前验证需关闭 E8M0 和 FlashInfer blockscale FP8 路径
