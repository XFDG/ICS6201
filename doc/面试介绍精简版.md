# 面试介绍精简版

面试官您好，我叫冯浩然，目前在香港中文大学（深圳）攻读集成电路与系统硕士，本科毕业于山东大学。我主要关注 GPU 高性能算子开发和大模型训练/推理系统优化。

目前我在九坤投资 AI 研究院实习，参与内部 300B 级 MoE 基座模型的开发与优化，工作覆盖训练算子、推理算子，以及 RL 训推一致性与 rollout 稳定性三条主线。

训练侧，我定位到长序列 deterministic Attention backward 的主要回退来自 dQ semaphore 依赖链，并在原 fused kernel 内对齐 contributor-relative ticket 与 reverse scheduler；代表 shape 完整 backward 从 6.755 ms 降至 1.699 ms，提升 3.98 倍，并通过 3,601 项回归和 1,000 次逐位确定性测试。另针对 Sink FC1 与通信竞争问题，我构建 4×H200 双 stream 代理，通过 NCCL CTA 预算和 DeepGEMM SM budget 控核，完成 121 组粗扫、89 组细扫；AllGatherV 代理中，联合 span 降低 12.80%，其中控核独立贡献 7.58%。

推理侧，我在 vLLM 中工程化接入确定性 Router GEMM 的多后端选择、CUDA Graph preflight、cache/fallback 和 workspace 生命周期管理，使 48 层 Router GEMM 中位耗时下降 73.39%，并完成 kernel 与模型级逐位一致性验证；同时完成 OE 异步状态算子，将历史 token 构造保留在 GPU，TP1 吞吐较同步路径提升 4.99%，TP2/TP4 均达到 0 mismatch。

RL 侧，我打通 R3 Router Replay 的采集、回放和指标闭环，在 8 张 H200 上将 route mismatch 从 17%–19% 降至 0，并扩展至 128 卡、内部 300B 级模型的 200-step rollout；另针对 TP=2 FULL CUDA Graph rollout hang 构建两卡最小复现，定位 FlashInfer 中 FTZ 导致 Lamport sentinel 误判，回移位级修复后 Graph replay 不再出现 hang 或 timeout。

此前在摩尔线程，我负责 TensorFlow MUSA Extension 的算子、图优化和稳定性工作：实现并修复 GELU 融合，使整网 11 个 GELU 全部融合、真实 shape 耗时降低 36.6%；也修复了 shape tensor HostMemory 路径导致的长跑随机崩溃。

这些经历让我形成了从最小复现、源码和 Kernel 定位，到正确性回归、性能剖析和多卡验证的工作方法。我希望继续从事 AI Infra、GPU 算子和大模型训练推理系统相关工作。谢谢。
